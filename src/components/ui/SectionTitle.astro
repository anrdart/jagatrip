---
interface Props {
  as?:     'h1' | 'h2' | 'h3';
  class?:  string;
  center?: boolean;
}
const { as: Tag = 'h2', class: cls = '', center = false } = Astro.props;
---
<Tag class={`section-title ${center ? 'text-center' : ''} ${cls}`}>
  <slot />
</Tag>
